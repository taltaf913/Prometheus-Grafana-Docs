global:
  scrape_interval:     15s # Scrape every 15 seconds.

scrape_configs:
  - job_name: 'streamlit-translation'
    static_configs:
      - targets: ['localhost:8000'] # Adjust if your app is running elsewhere
